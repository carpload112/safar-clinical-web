import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'Studies' }
export default function StudiesLayout({ children }: { children: React.ReactNode }) { return <>{children}</> }
